[project]
name = "ingestion-service"
version = "0.1.0"
description = "Event ingestion service with idempotency and validation"
readme = "RUNBOOK.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "mypy>=2.3.1",
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
